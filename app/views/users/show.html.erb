<%= @user.id %><br>
<%= @user.email %>


<%= @user.followers.count %><br>

<%= @user.followings.count %>

<% if @relation.present? %>
  <%= button_to 'Unfollow', followers_relationship_path(@relation.id), method: :delete %>
<% else %>
  <%= button_to 'Follow', user_followers_relationships_path(@user), method: :post %>
<% end %>
